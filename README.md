# Sentiment analysis



```
# install python
sudo apt-get update
sudo apt install  python3.6  python3.6-venv

# create and activate  virtual env
# [PyCharm]  remember to setup intepreter on this virtual env, not global python
python3.6 -m venv <DIR>
cd  ~/<DIR>/bin/; . activate.fish


# inmarkall project dependencies
pip install -r requirements.txt
```